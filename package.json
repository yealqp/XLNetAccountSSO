{
  "name": "sso-platform",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev:web": "pnpm --filter @sso/web dev",
    "build:web": "pnpm --filter @sso/web build",
    "build:server": "set GOOS=linux&& set GOARCH=amd64&& go -C server build -o xlnet-account-server ./cmd/sso"
  }
}
